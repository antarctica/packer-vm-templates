{
  "variables": {
    "iso_url": "http://mirrors.kernel.org/centos/6.5/isos/x86_64/CentOS-6.5-x86_64-bin-DVD1.iso",
    "iso_checksum": "32c7695b97f7dcd1f59a77a71f64f2957dddf738",
    "iso_checksum_type": "sha1",
    "io_ram_size": 480,
    "hostname": "vagrant",
    "ssh_username": "vagrant",
    "ssh_password": "vagrant",
    "distro": "centos-6.5-x86_64"
  },
  "builders": [
    {
      "type": "virtualbox-iso",

      "format": "ova",
      "vm_name": "virtualbox",
      "guest_os_type": "RedHat_64",

      "virtualbox_version_file": ".vbox_version",
      "guest_additions_path": "VBoxGuestAdditions_{{.Version}}.iso",

      "vboxmanage": [
        [
          "modifyvm",
          "{{.Name}}",
          "--memory",
          "{{user `io_ram_size`}}"
        ],
        [
          "modifyvm",
          "{{.Name}}",
          "--cpus",
          "1"
        ]
      ],
      "disk_size": 40960,

      "iso_url": "{{user `iso_url`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",

      "http_directory": "../preseed",

      "boot_command": [
        "<tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `distro`}}/ks.cfg<enter><wait>"
      ],
      "boot_wait": "10s",

      "ssh_password": "{{user `ssh_username`}}",
      "ssh_port": 22,
      "ssh_username": "{{user `ssh_password`}}",
      "ssh_wait_timeout": "10000s",

      "shutdown_command": "echo 'vagrant'|sudo -S /sbin/halt -h -p",

      "output_directory": "packer-{{user `distro`}}-virtualbox-iso"
    },
    {
      "type": "vmware-iso",

      "vm_name": "vmware",
      "guest_os_type": "centos-64",

      "tools_upload_flavor": "linux",
      
      "vmx_data": {
        "cpuid.coresPerSocket": "1",
        "memsize": "{{user `io_ram_size`}}",
        "numvcpus": "1"
      },
      "disk_size": 40960,

      "iso_url": "{{user `iso_url`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",
      "http_directory": "../preseed",

      "boot_command": [
        "<tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{user `distro`}}/ks.cfg<enter><wait>"
      ],
      "boot_wait": "10s",

      "ssh_password": "{{user `ssh_username`}}",
      "ssh_port": 22,
      "ssh_username": "{{user `ssh_password`}}",
      "ssh_wait_timeout": "10000s",

      "shutdown_command": "echo 'vagrant'|sudo -S /sbin/halt -h -p",

      "output_directory": "packer-{{user `distro`}}-vmware-iso"
    }
  ],
  "provisioners": [
    {
      "execute_command": "echo 'vagrant' | {{.Vars}} sudo -S -E bash '{{.Path}}'",
      "scripts": [
        "../provisioining/scripts/centos/fix-slow-dns.sh",
        "../provisioining/scripts/common/sshd.sh",
        "../provisioining/scripts/common/vagrant.sh",
        "../provisioining/scripts/common/vmtools.sh",
        "../provisioining/scripts/centos/cleanup.sh",
        "../provisioining/scripts/common/minimize.sh"
      ],
      "type": "shell"
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "output": "../base-boxes/boxes/{{user `distro`}}/{{.Provider}}.box"
    }
  ]
}

